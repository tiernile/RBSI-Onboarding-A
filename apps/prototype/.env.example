# Copy this file to .env (for build/start) or .env.development (for dev)

# Admin password hash for login (bcrypt). Generate with: pnpm hash "YourPassword"
# Example (do not use in production):
# NUXT_ADMIN_PASSWORD_HASH=$2a$12$XXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXX

# Optional: Data directory override (defaults to repo data/)
# NUXT_DATA_DIR=/absolute/path/to/repo/data

# Optional: Mission Control visibility overrides
# MC_VISIBLE=non-lux-lp-demo
# MC_STATUS={"non-lux-lp-demo":"alpha"}
